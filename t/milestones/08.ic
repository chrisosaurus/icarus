fn is_div(a::Uint, b::Uint) -> Bool
    # interesting inference case here for literal `0`
    return (a % b) == 0
end

fn main()
    if is_div(1, 1)
      println("is_div(1, 1)")
    end

    if not is_div(1, 2)
      println("!is_div(1, 2)")
    end
end
