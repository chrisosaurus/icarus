
fn is_div(a::Int, b::Int) -> Bool
    return not(a % b)
end

fn fizzbuzz(from::Int, to::Int)
    for num in range(from to)
        let &arr::Vector<String>

        if is_div(num, 3) then
            &arr.append("Fizz")
        end

        if is_div(num, 5) then
            &arr.append("Buzz")
        end

        if length(arr) == 0 then
            &arr.append(num)
        end

        print(join(" ", arr))
    end
end

fn main()
    fizzbuzz(1, 100)
end

